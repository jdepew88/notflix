# Server
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Public URL (used for absolute links; set to your Traefik domain in production)
NEXT_PUBLIC_APP_URL=http://10.10.0.8:3233

# Persistent settings (merged with .env on startup)
DATA_PATH=/app/data

# Plex (required for library playback)
PLEX_URL=http://10.10.0.8:32800
PLEX_TOKEN=

# NFS library mount inside the container (host /mnt/user/Media/Video → /media/Video)
LIBRARY_PATH=/media/Video

# Session cookies — set false for HTTP (e.g. http://10.10.0.8:3233)
COOKIE_SECURE=false
SESSION_SECRET=change-me-to-a-random-string

# Optional metadata APIs
TMDB_API_KEY=
TVDB_API_KEY=

# Optional Real-Debrid (enables Torrentio fallback when title is not in Plex)
REAL_DEBRID_TOKEN=

# Optional: paste your full Torrentio install URL from https://torrentio.strem.fun/configure
# (Copy Link on Install — remove /manifest.json). If unset, built from REAL_DEBRID_TOKEN.
# TORRENTIO_URL=https://torrentio.strem.fun/realdebrid=YOUR_TOKEN|sort=quality|qualityfilter=480p,scr,cam,unknown|language=english

# Optional Peerflix addon URL from https://config.peerflix.mov (English filter applied in-app if omitted)
# PEERFLIX_URL=https://peerflix.mov

# Optional: override ffmpeg paths (installed in container by default)
# FFMPEG_PATH=/usr/bin/ffmpeg
# FFPROBE_PATH=/usr/bin/ffprobe
